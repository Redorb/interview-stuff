import React from 'react';
import NavigationBar from '../NavigationBar/NavigationBar'

const Header = props => {
  return (
    <div>
      <NavigationBar />
    </div>
  );
};

export default Header;